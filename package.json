{
  "name": "new-website-main",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "install": "npm --prefix frontend install --legacy-peer-deps && npm --prefix backend_node install",
    "build": "npm --prefix frontend run build",
    "prestart": "npm run build",
    "start": "npm --prefix backend_node start",
    "dev": "npm --prefix frontend start"
  }
}
